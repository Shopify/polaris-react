---
title: Divider
category: Layout and structure
webComponent:
  name: s-divider
  url: https://shopify.dev/docs/api/app-home/polaris-web-components/structure/divider
  type: polaris
keywords:
  - layout
  - divider
  - border
examples:
  - fileName: divider-with-border-color.tsx
    title: Color
    description: Divider border color can be adjusted using the [Color tokens](https://polaris.shopify.com/tokens/color).
previewImg: /images/components/layout-and-structure/divider.png
---

# {frontmatter.title}

<Lede>

Use to separate or group content.

</Lede>

<WebComponentBanner
  componentName={frontmatter.title}
  webComponentName={frontmatter.webComponent.name}
  webComponentUrl={frontmatter.webComponent.url}
  type={frontmatter.webComponent.type}
/>

<Examples />

<Props componentName={frontmatter.title} />
